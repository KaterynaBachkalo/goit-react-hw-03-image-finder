import { StyledButton } from './Button.styled';

export const Button = () => {
  return (
    <StyledButton>
      <button type="button" className="button">
        Load more
      </button>
    </StyledButton>
  );
};
